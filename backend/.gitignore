.vercel

# Supports de cours source (PDF convertis en texte) utilisés pour rédiger le contenu — scratch,
# pas du code, gardés en local uniquement.
tmp-*.txt
.dev-pg-data/

# Variables d’environnement de production récupérées via `vercel env pull` — JAMAIS versionnées.
.env.prod
.env*.local
